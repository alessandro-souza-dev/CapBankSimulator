// preload.js
// Este arquivo é carregado antes do renderer process
// Pode ser usado para expor APIs seguras do Node.js para o renderer

// Por enquanto, mantemos vazio como boa prática de segurança
console.log('Preload script carregado');
